Feature: showing off CEDAR

  @episode
  Scenario: run a simple test
     Given we have saved an episode
      when we implement a test
      then behave will test it for us!

  @subrecord
  Scenario: run a simple test
     Given we have saved a subrecord
      when we implement a test
      then behave will test it for us!
